Left: two image stacks, image A and image B, each with an app layer above a shared base layer, the two base layers joined to show they are one stored layer named by both manifests. Right: a card showing a truncated digest sha256:9f2c...be41 labelled content hash and immutable, with two identical :v2 tag boxes below it — one labelled today connected by a solid arrow, one labelled next week connected by a dashed arrow to a different point, showing that a tag is a label that can be moved to point at a different image.
