chapter: 8
book: kcna
generated: '2026-09-01T07:29:27+00:00'
figures:
- anchor_id: ch08-fig01-kubectl-verb-resource-grammar
  status: approved
  render_paths:
    svg: figures/ch08-fig01-kubectl-verb-resource-grammar.svg
    png: figures/ch08-fig01-kubectl-verb-resource-grammar.png
  source_path: figures/sources/ch08-fig01-kubectl-verb-resource-grammar.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The five-column table cleanly maps each example command onto the kubectl/[command]/[TYPE]/[NAME]/[flags]
      grammar, with empty-slot cells rendered as de-emphasized dashes so readers see
      at a glance which slots are optional per verb. The TYPE and NAME columns get
      a tinted background band plus upward arrows anchoring to bold brass-colored
      'case-INsensitive' / 'case-SENSITIVE' captions with concrete pod/Node-7 examples,
      satisfying the fixed-point requirement with both color and non-color (arrow
      + bold + column shading) signals.
    suggested_fix: ''
- anchor_id: ch08-fig02-three-api-gates
  status: approved
  render_paths:
    svg: figures/ch08-fig02-three-api-gates.svg
    png: figures/ch08-fig02-three-api-gates.png
  source_path: figures/sources/ch08-fig02-three-api-gates.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: concerns
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The vertical gate sequence with a REJECT branch off each stage and
      a REWRITTEN loop only off Admission communicates the learning outcome clearly
      and correctly singles out Admission as the mutating gate, with the Brass dashed
      REWRITTEN path (color + line-style non-color signal) rejoining cleanly above
      'Persisted to etcd'. However the render carries 10 distinct text labels (REQUEST,
      GATE 1, Authentication, REJECT, GATE 2, Authorization, GATE 3, Admission, REWRITTEN,
      Persisted to etcd), exceeding the locked ≤7 working-memory ceiling, mainly because
      each gate has both a 'GATE N' badge and a separate name label.
    suggested_fix: Merge each gate's badge and name into a single label (e.g. 'GATE
      1 · Authentication') to cut the distinct-label count from 10 to 7.
- anchor_id: ch08-fig05-quota-vs-limitrange
  status: pending
  render_paths:
    svg: figures/ch08-fig05-quota-vs-limitrange.svg
    png: figures/ch08-fig05-quota-vs-limitrange.png
  source_path: figures/sources/ch08-fig05-quota-vs-limitrange.html
  validation_results:
    label_count: pass
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: Lay out the two panels side by side (ResourceQuota left, LimitRange
    right) instead of stacked top/bottom, per the spec's alt_text_seed.
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: concerns
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: 'The two-panel layout, dashed brass namespace border on the left (absent
      on the right), the four-pod grid, and the ''AT CAP'' bar all correctly communicate
      the quota-vs-scope distinction. However, both fifth-pod arrival boxes at the
      bottom render as empty dashed placeholders with no content — the left should
      show a visibly rejected/blocked pod (e.g. oxblood mark or X) and the right should
      show the LimitRange defaults actually filled in (e.g. ''min: 100m / max: 500m''),
      which is the specific mechanism the learning outcome hinges on for the LimitRange
      half.'
    suggested_fix: 'Fill the two arrival-pod placeholder boxes with real content:
      an oxblood rejection glyph/X plus ''quota exceeded'' on the left, and pod spec
      text showing auto-filled min/max default values on the right.'
- anchor_id: ch08-fig04-node-lifecycle-cordon-drain
  status: approved
  render_paths:
    svg: figures/ch08-fig04-node-lifecycle-cordon-drain.svg
    png: figures/ch08-fig04-node-lifecycle-cordon-drain.png
  source_path: figures/sources/ch08-fig04-node-lifecycle-cordon-drain.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The four-state sequence (Schedulable → Cordoned → Drained → Schedulable)
      with kubectl transition labels and explicit admitted/blocked new-pod arrows
      clearly teaches the cordon/drain/uncordon lifecycle, reinforced by the callout
      text stating cordon blocks new pods without eviction. Pods A, B, C are rendered
      identically in the Schedulable and Cordoned panels and are grouped inside a
      brass-bordered container (color + distinct nested-border signal) that matches
      the legend's 'pods unchanged' swatch, satisfying the fixed-point requirement.
    suggested_fix: ''
- anchor_id: ch08-fig03-version-skew-window
  status: approved
  render_paths:
    svg: figures/ch08-fig03-version-skew-window.svg
    png: figures/ch08-fig03-version-skew-window.png
  source_path: figures/sources/ch08-fig03-version-skew-window.html
  validation_results:
    label_count: fail
    palette_conformance: fail
    min_font_size: fail
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: 'The horizontal range chart clearly shows each component''s version-skew
      tolerance relative to kube-apiserver (double line at 0): kubelet/kube-proxy
      reach back 3, controller-manager/scheduler/cloud-controller-manager reach back
      1, and kubectl alone spans -1 to +1, crossing the reference line — this directly
      conveys which components may disagree, by how much, and in which direction.'
    fixed_point_visible_detail: kubectl bar uses Brass fill/border/endpoints versus
      Teal/gray for all other bars, and is additionally distinguished structurally
      by being the only bar that crosses the double reference line to the right of
      0.
    suggested_fix: ''
- anchor_id: ch08-zenith-consequences-not-rules
  status: approved
  render_paths:
    svg: figures/ch08-zenith-consequences-not-rules.svg
    png: figures/ch08-zenith-consequences-not-rules.png
  source_path: figures/sources/ch08-zenith-consequences-not-rules.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: 'Collapse the two column headers into a single caption/subtitle
    and merge the four right-side controller boxes into one grouped box (e.g. ''downstream
    consumers: scheduler / node controller / workload controllers / control loop'')
    to bring the label count to ≤7.'
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: Four administrative actions converge with clean arrows on the API Server
      box, which carries both the brass border and an explicit 'ONE DOOR' badge —
      an unambiguous fixed point. The design collapses the four downstream consumers
      into one grouped list box (with chapter callbacks) plus a separate etcd arrow
      rather than four discrete arrows as the alt-text literally describes, but this
      stays within the 7-label cap and still reads as 'one door, then reconciled by
      controllers you've already met,' which is the actual learning outcome.
    suggested_fix: ''
