chapter: 10
book: kcna
generated: '2026-09-01T17:14:23+00:00'
figures:
- anchor_id: ch10-fig02-ingress-fanout-and-name-based-hosts
  status: approved
  render_paths:
    svg: figures/ch10-fig02-ingress-fanout-and-name-based-hosts.svg
    png: figures/ch10-fig02-ingress-fanout-and-name-based-hosts.png
  source_path: figures/sources/ch10-fig02-ingress-fanout-and-name-based-hosts.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: 'The two-panel layout holds everything constant (client IP, request
      count, service count) except the underlined fragment and the decision-box label,
      which makes the path-vs-host distinction legible at a glance and the footer
      caption reinforces the exact learning outcome. The fixed point is doubly marked:
      the matched fragment is underlined in Brass and the ''reads: path''/''reads:
      host'' boxes carry a Brass border plus bold text, both distinguished from the
      Teal/Navy borders used elsewhere. The left-to-right request-to-decision-to-fork
      arrangement in each panel is a clean, correctly structured data-flow diagram.'
    suggested_fix: ''
- anchor_id: ch10-fig03-gateway-api-role-split
  status: regenerate
  render_paths:
    svg: figures/ch10-fig03-gateway-api-role-split.svg
    png: figures/ch10-fig03-gateway-api-role-split.png
  source_path: figures/sources/ch10-fig03-gateway-api-role-split.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: Give the 'exactly 1' and 'many (parentRefs)' edges a visibly thicker
    brass stroke (or a double-line/dash pattern) than the diagram's other connectors
    so the fixed point reads without relying on color alone.
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: concerns
    matches_intent: true
    fixed_point_visible: false
    structure_correct: true
    reasoning: 'The three-band hierarchy is clean and legible, correctly nests GatewayClass/Gateway/HTTPRoute
      under their owning roles, and the caption reinforces the ownership-vs-runtime
      distinction from the alt text. However, the two cardinality edges/labels rely
      on brass color alone — stroke weight matches the rest of the diagram''s lines
      and the uppercase letter-spacing on ''EXACTLY 1''/''MANY (parentRefs)'' is the
      same typographic treatment used for the (navy) band headers, so there''s no
      second, color-independent signal marking them as the fixed point. Also worth
      noting: the diagram carries 8 distinct labels (3 bands + GatewayClass + Gateway
      + HTTPRoute + 2 cardinality labels), one over the ≤7 working-memory cap.'
    suggested_fix: Give the 'exactly 1' and 'many (parentRefs)' edges a visibly thicker
      brass stroke (or a double-line/dash pattern) than the diagram's other connectors
      so the fixed point reads without relying on color alone.
- anchor_id: ch10-fig04-networkpolicy-additive-selectors
  status: approved
  render_paths:
    svg: figures/ch10-fig04-networkpolicy-additive-selectors.svg
    png: figures/ch10-fig04-networkpolicy-additive-selectors.png
  source_path: figures/sources/ch10-fig04-networkpolicy-additive-selectors.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The convergence of Policy A and Policy B onto a single Pod, followed
      by one arrow into the brass-filled 'permitted set' blob annotated '(one set,
      two grants)', directly visualizes additive/union selector semantics, and the
      separate dashed, fog-colored 'not granted' box with 'not denied — simply never
      granted' cleanly conveys the second half of the learning outcome. The permitted-set
      blob is distinguished by both brass color and a solid fill unique among all
      boxes in the diagram (all others are white or dashed-outline), giving a non-color
      redundant cue.
    suggested_fix: ''
- anchor_id: ch10-zenith-nothing-without-a-controller
  status: approved
  render_paths:
    svg: figures/ch10-zenith-nothing-without-a-controller.svg
    png: figures/ch10-zenith-nothing-without-a-controller.png
  source_path: figures/sources/ch10-zenith-nothing-without-a-controller.html
  validation_results:
    label_count: pass
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The four side-by-side panels each show a valid object, a ghosted/dashed
      'absent component' row marked (none), and a bottom outcome row of 'nothing'
      — this reads instantly as the same failure pattern recurring across four unrelated
      Kubernetes objects, matching the alt_text_seed's described layout exactly. Panel
      four's outcome box is the only one with a filled border, and both its border
      and text are rendered in Brass against the other panels' plain unboxed text,
      giving the fixed point two distinct signals (color + boxed/bordered treatment).
    suggested_fix: ''
