chapter: 16
book: kcna
generated: '2026-09-01T16:28:32+00:00'
figures:
- anchor_id: ch16-fig01-application-scope-triage
  status: approved
  render_paths:
    svg: figures/ch16-fig01-application-scope-triage.svg
    png: figures/ch16-fig01-application-scope-triage.png
  source_path: figures/sources/ch16-fig01-application-scope-triage.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The root 'Application scope' box with the incoming Ch13 handoff arrow,
      and four question nodes fanning out in the exact order specified (Running, Healthy/Configured,
      Reachable, Per-replica) with correct section refs, closely match the learning
      outcome and alt-text seed; the 'top to bottom, each question eliminates ground'
      caption reinforces the triage-order intent.
    suggested_fix: ''
- anchor_id: ch16-fig05-init-sequence-debug-points
  status: approved
  render_paths:
    svg: figures/ch16-fig05-init-sequence-debug-points.svg
    png: figures/ch16-fig05-init-sequence-debug-points.png
  source_path: figures/sources/ch16-fig05-init-sequence-debug-points.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: Render the Init:N/M and Running status labels inside an actual
    brass-bordered or brass-filled badge/pill (matching what the legend already promises)
    instead of plain brass-colored text, so the fixed point has a non-color signal
    too.
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The left-to-right init-1 → init-2 → init-3 → app containers sequence
      with Init:0/3, Init:1/3, Init:2/3, Running pills aligned directly beneath each
      box makes the Init:N/M reading skill immediately graspable, and the three failure-signature/cause
      pairs below map cleanly onto the learning outcome. The status pills use a distinct
      Brass outline plus a pill shape and bold text versus the container boxes' navy
      rounded-rect outline, giving the fixed-point emphasis both a color and non-color
      signal.
    suggested_fix: ''
- anchor_id: ch16-fig02-ephemeral-container-debug
  status: approved
  render_paths:
    svg: figures/ch16-fig02-ephemeral-container-debug.svg
    png: figures/ch16-fig02-ephemeral-container-debug.png
  source_path: figures/sources/ch16-fig02-ephemeral-container-debug.html
  validation_results:
    label_count: fail
    palette_conformance: fail
    min_font_size: fail
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: Each of the three panels states the specific question that debug shape
      answers ('what does the running process see right now?', 'what would happen
      if I changed something?', 'is the machine itself the problem?'), which directly
      teaches the decision criterion from the learning outcome, and panel B's original
      vs. copy comparison makes the --copy-to non-mutation clear. Containment is correctly
      nested (Node > Pod > container) matching K8s topology.
    suggested_fix: ''
- anchor_id: ch16-fig04-service-break-points
  status: approved  # 2026-09-05 coordinator review: breaks 1 and 2 now distinguished (slice empty vs ready: false); promoted
  render_paths:
    svg: figures/ch16-fig04-service-break-points.svg
    png: figures/ch16-fig04-service-break-points.png
  source_path: figures/sources/ch16-fig04-service-break-points.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: 'CORRECTION (audit P1): the previous render said ''BREAK 1+2 -
    LIST EMPTY'' and its legend said upstream breaks empty the list. That is wrong,
    and the source ASCII has been corrected and is the authority. The EndpointSlice
    callout must read ''BREAK 1+2 - NO READY ENDPOINTS'' with two numbered causes:
    ''1 selector mismatch -> slice empty'' and ''2 not Ready -> ready: false'' (matching
    Pods STAY in the slice, marked ready: false). The BREAK 3 callout must say ''(endpoints
    ARE ready)'', not ''list is populated''. The three legend rows must read exactly:
    ''UPSTREAM of readiness ... breaks 1 and 2 -> NO READY ENDPOINTS''; ''DOWNSTREAM
    of readiness ... break 3 -> ENDPOINTS READY, request still fails''; ''BESIDE the
    whole path ... break 4 -> you never reached this Service at all''. The words ''list
    empty'', ''empty list'' and ''populated'' must not appear anywhere. Keep the left-to-right
    chain client -> DNS name -> Service -> EndpointSlice -> Pod -> container port
    with the three callouts dropping below it, and brass on the BREAK 1+2 callout
    only.'
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The left-to-right request path (client -> DNS name -> Service -> EndpointSlice
      -> Pod -> container port) is clear and linear, matching the data_flow type,
      and each callout box lands beside the correct node with a legend that correctly
      frames upstream/downstream/beside semantics relative to the endpoint list.
    fixed_point_visible_detail: The BREAK 1+2 / LIST EMPTY callout is the only one
      filled with Brass and has a solid Brass border plus a solid (not dashed) Brass
      connector line to EndpointSlice, both non-color signals distinguishing it from
      the two dashed, unfilled Fog-bordered callouts.
    suggested_fix: ''
- anchor_id: ch16-fig03-portforward-vs-service-path
  status: approved  # 2026-09-04 coordinator review: render matches the chapter; promoted to figures/ and applied
  render_paths:
    svg: figures/ch16-fig03-portforward-vs-service-path.svg
    png: figures/ch16-fig03-portforward-vs-service-path.png
  source_path: figures/sources/ch16-fig03-portforward-vs-service-path.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: 'The source ASCII has been revised since the last render and is
    the authority. The lower (port-forward) path is kubectl -> API server -> Pod:port,
    with the note ''pods/portforward subresource'' under the API server. Do NOT draw
    a kubelet node: there is no kubelet hop in this figure. In the upper (Service)
    path the fourth node is ''service proxy'' with the subtitle ''kube-proxy or its
    equivalent'' (not ''kube-proxy rules''). Keep the two Pod nodes as the only shared
    step and the only brass-accented elements, keep the note that all of section 4''s
    breaks 1-4 live on the Service path, and keep the note that the port-forward path
    shares no step with the Service path except the Pod. Prefer node subtitles over
    separate floating callout boxes so the label count stays low.'
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: concerns
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The two-column layout cleanly shows the Service path (client-DNS-Service-proxy-Pod)
      and the port-forward path (kubectl-API server-kubelet-Pod) converging only at
      the Pod, with a dashed 'same pod, diff. port' connector making the shared-step
      argument for fault localization immediately legible. However the diagram carries
      9 node boxes with 8 distinct titles (client, DNS, Service, service proxy, Pod,
      kubectl, API server, kubelet) plus 11 subtitle annotations, exceeding the locked
      ≤7-label working-memory ceiling and adding more text than the learning outcome
      strictly needs.
    suggested_fix: Drop or merge low-value subtitle annotations (e.g. fold 'sends
      the request'/'resolves the name'/'streams connection' into a caption instead
      of per-node) to bring the diagram under the 7-label ceiling while keeping the
      two Pod nodes as the sole brass-highlighted shared step.
- anchor_id: ch16-zenith-mine-or-the-platforms
  status: approved
  render_paths:
    svg: figures/ch16-zenith-mine-or-the-platforms.svg
    png: figures/ch16-zenith-mine-or-the-platforms.png
  source_path: figures/sources/ch16-zenith-mine-or-the-platforms.html
  validation_results:
    label_count: fail
    palette_conformance: pass
    min_font_size: pass
    max_dimensions: pass
    aspect_ratio: pass
    grayscale_legibility: pass
    alt_text_present: pass
  regenerate_hint: ''
  review_notes: ''
  last_applied_hash: null
  ai_review:
    verdict: looks_good
    matches_intent: true
    fixed_point_visible: true
    structure_correct: true
    reasoning: The symmetrical two-column layout visually equates platform-scope (phase→conditions→events→logs→node)
      and application-scope (running?→healthy?→reachable?→configured?→which replica?)
      narrowing chains, and both 'NARROWING' arrows converge on a single boundary
      reinforced by 'THE BOUNDARY / this line is the method' caption, which directly
      communicates that these are one method meeting at an ownership boundary rather
      than two subjects.
    suggested_fix: ''
