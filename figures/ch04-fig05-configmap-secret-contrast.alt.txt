A four-row comparison of ConfigMap and Secret covering intended contents, how a Pod consumes them, how they are stored, and what each adds. The storage row reads unencrypted in etcd for both, with by default noted on the Secret side.
