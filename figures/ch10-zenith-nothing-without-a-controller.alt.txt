Four panels in a two-by-two grid, each with three rows. The top row names a valid object: a Service of type LoadBalancer, a Service with an empty selector, an Ingress with host and path rules, and a NetworkPolicy with a pod selector, each marked valid with a check. The middle row of each panel is a dashed, grayed box naming the absent component — provider, matching Pods, Ingress controller, network plugin — each marked none. The bottom row reads nothing in all four panels; in the fourth panel that row is highlighted and adds the words and nothing tells you. A caption reads: an object without its component does nothing. A legend keys a solid outline to valid, a dashed outline to absent, and the highlight to silent.
